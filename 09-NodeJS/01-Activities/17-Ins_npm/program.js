const inquirer = require("inquirer");

//type "npm init -y" in the terminal to create a package.json file
//package.json is the metadata of your application
//package.json shows all the dependencies needed for the application

//package-lock.json stores all the sub-dependencies for the application
//package.json stores the overall dependencies needed
